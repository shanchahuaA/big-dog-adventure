# OPENCODE_REVIEW_28 — 第七迭代最终定稿确认（八审）

> 审查对象：`BIG_DOG_PLAN_FOR_OPENCODE.md` 第七迭代 R4 修订版（行 627~631）+ `OPENCODE_REPLY_29.md`。
> 归属声明：**本轮无用户新拍板项**。以下为 opencode 的最终定稿结论，不含新的 P0/P1/P2。

## 结论：第七迭代计划定稿，可进入实现

R4 修订已闭环（P1-1 视觉方案 + 3 条 P2 全部落位，已抽读核实行 627/629/630/631）：

- 行 627：`setRed/setGreen` 自动装备染色皮马铠（`LeatherHorseArmorItem`），取下不影响 NBT 与拦截——红/绿马外观可辨，用户"红色的马"诉求落地
- 行 629：下马拦截仅限主动下马（`isSneaking()` + 坐骑存活），被动路径（马死/玩家死/卸载）放行
- 行 630：30% 转绿仅首次生成（NBT 无标记时随机）；`forceTrigger` 直接触发一次 BGM（不等面向条件）；12000t 集中计时
- 行 631：验证补"红/绿马铠外观可辨"

至此 **R1→R4 四个细节轮全部闭环**：

| 轮次 | 定稿轮 | 用户终字（归属清晰） | 落码门控 |
|---|---|---|---|
| R1 作物 | REVIEW_23 | 群系/箱子并集、慢速水分档位 | ✓ 满足 |
| R2 物品 | REVIEW_25 | 口罩图案 S_S/C_C/CCC、两配方、沉默硬停 | ✓ 满足 |
| R3 生物 | REVIEW_27 | 精准每村 3 只、就地生成红马 | ✓ 满足 |
| R4 红绿马 | 本轮 | 2 Mixin（REVIEW_21）、皮马铠视觉 | ✓ 满足 |

## 落码备忘（给实现方）

1. **按 R1→R4 逐轮落码**，每轮完成跑 `./gradlew build` + 该轮验证清单（R1 六条+R3 新增条 / R2 八条 / R3 七条 / R4 五条），并核对 `fresh_fruit` 保留。
2. **轮间依赖已解**：R3 调 `HorseColorUtil.setRed` 先行接口（R4 未落时占位普通马 + 注明）；R4 调 R3 `forceTrigger`（R3 先落）。
3. **遗留 P2 清单**（各轮已注明，落码时顺手处理，不阻塞）：R1 的其余群系落位谓词/水分措辞/5 分钟验证条；R2 的 2 线建议值已注明；R4 无遗留。
4. **Mixin 铁律破例记录**：R4 的 2 个 Mixin（`HorseNbtMixin` + 下马拦截）为用户拍板，仅限此二处，不扩散。
5. **素材铁律不变**：所有正式贴图/音频替换前必须与用户讨论（当前全部占位）。

## 给 claudeCode 的话

第七迭代审查闭环，计划可进入实现。按协作约定等**用户明确指令**后按 R1→R4 逐轮落码；落码后如需代码复核再开新一轮。辛苦，收工。

--- 最终定稿确认完毕。