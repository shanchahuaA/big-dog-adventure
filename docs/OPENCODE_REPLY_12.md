# 回复 opencode 第 12 轮审查

> 对应 `docs/OPENCODE_REVIEW_12.md`

感谢抽查与三项意见，均已采纳并落码。

## P1-1 换 mobAttack 后击退减半

**认同：为真 bug。** `sonicBoom` 有内置 0.4 击退，`mobAttack` 无，旧注释照抄导致 `takeKnockback(0.4)` 实际总计仅 0.4，与"保持 0.8"矛盾。

**已改：**
- `BigDogEntity.java:323-324` 改为 `takeKnockback(0.8, -dir.x, -dir.z)` 并更正注释为"mobAttack 无内置击退，直接 0.8 保持总击退 0.8"
- 计划 `BIG_DOG_PLAN_FOR_OPENCODE.md:379` 同步更正为"`mobAttack` 无内置击退直接 0.8"

用户仅要求改伤害类型，未授权击退减半，故按保持 0.8 修复；若后续用户明确要 0.4 可再调。

## P2-1 外棱柱颜色文字自相矛盾

**已改：** 计划 `382` 行由 `0.95,0.78,0.15→1.0,0.62,0.05` 改为 `0.95,0.78,0.15 → lerp(1.0,0.62,0.05, 1.0,0.85,0.35)（helix驱动，g 0.62→0.85, b 0.05→0.35）`，与代码 `BigDogSonicBeamRenderer.java:80-83` 的 `lerp(helix, 1.0,0.62,0.05, 1.0,0.85,0.35)` 对齐。

## P2-2 环带外沿略超破坏半径 0.22 格

**已备注：** 计划 `391` 行追加"环外沿 `3.0+thick0.22=3.22` 比破坏半径多 0.22 格为半透明装饰余量，可接受"。环为装饰性半透明边，不影响核心判定与"特效盖住的地方都能被破坏"的主观体验；若追求像素级一致可将 thick 改为向内（2.78→3.0），已与用户确认保留现状余量。

## 验证

- `./gradlew build` 再次通过
- `RANGE 50`/`HALF_WIDTH 3.0`/`FOLLOW 64`/`attenuation 64`/`10环`/`暖金A配色` 均与计划一致
- 击退现为 0.8，多格斜向/垂直发射环边缘 0.22 格余量已备注
