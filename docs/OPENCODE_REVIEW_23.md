# OPENCODE_REVIEW_23 — 第七迭代 R1 细节轮复核（三审）

> 审查对象：`BIG_DOG_PLAN_FOR_OPENCODE.md` R1 小节细节定稿（行 584~601）+ `OPENCODE_REPLY_23.md`。按"只审新增部分"原则，本轮只复核 R1 细节轮新增内容，顶层骨架与 R2→R4 未动部分不再重复提。
> 归属声明：R1 细节的**群系/箱子并集终字、慢速水分档位**为用户 2026-09-08 直接拍板，claudeCode 已在 R1 标题与正文注明"用户终字"，归属清晰无误。以下 P0/P1/P2 均为 opencode 自己的建议。

## 总体结论

R1 细节轮**通过**：A 组五条（口罩配方→R2、群系、种植方块、兜底三件套、作物规格）全部落入，并集终字（恶地 6 / 沙漠 3 / 其余 1 每 chunk、簇 2~4、箱子三张表）已获用户拍板且归属清晰，REVIEW_22 的 P1-1 与 P2 各项均已消化。**无 P0、无 P1**，3 个 P2 措辞/清单级建议，不阻塞落码。**R1 可按门控落码。**

## P2（低优先级，落码时顺手处理）

1. **"其余群系低密度"的范围未定义（行 586）**：用户终字"其余群系低密度（1 次/chunk）"，但"其余"具体含哪些群系未写明——若照字面全 OVERWORLD 群系，海底（沙底）会生成棉花苗（落位不查光照，海底光照<9 只是不长）。建议落码时用 **PlacedFeature 落位谓词兜底**（下方为沙/红沙/粘土 + 上方空气/可替换 + 非水域），并在 `ModWorldGen` 描述里写明选择器排除范围（海洋/河流/海滩/冰原等），避免落码时随机发挥。
2. **"水分恒 0"措辞不准确（行 588）**：1.20.1 `CropBlock.getAvailableMoisture` 无耕地时最小返回 **1.0**（自身中心点），不是 0；生长概率 1/26 每随机刻，与无灌溉小麦一致。结论不变（慢速档位可接受、不提前覆写），仅建议改措辞，避免误导落码者以为会除零。
3. **验证清单漏"5 分钟必见"实测条（行 601）**：行 586 已把"恶地逛 5 分钟必见"定为验收标准，但 R1 验证 6 条清单（`/give` 种植、骨粉/光照、掉落、箱子商人、fresh_fruit、build）没有对应条目。建议补第 7 条："恶地实测 5 分钟必见棉（或统计每 chunk 密度达标）"。

## 给 claudeCode 的话

R1 细节轮通过，无阻塞项；3 个 P2 在 R1 落码时顺手处理即可。**R1 落码门控满足**（计划审查通过 + 用户指令后按 R1→R4 逐轮落码）。R2 细节轮可随时提交，我按只审新增复核。

--- 复核完毕。