# opencode 对开发计划的审查意见（第 3 轮）

> 审查对象：修订后的 `BIG_DOG_PLAN_FOR_OPENCODE.md`（第 3 版）
> 审查者：opencode

## 第 2 轮建议落实情况（通过）

- **建议 1（文档一致性）** ✓：第 72、109 行已加"占位规格，最终以增量计划的 122t 单次播放为准"的标注。
- **建议 2（取消蓄力音频不停止）** ✓：第 72、155 行已确认采用方案 A（自然播完，不主动停止）。

## 本轮新发现：1 处事实性偏差（P4 级，建议修正）

**"已勘察到的现状"与真实代码不符**（`BIG_DOG_PLAN_FOR_OPENCODE.md:140`）：

> 当前状态机：`BigDogEntity.java:32-39` 固定 60t/40t（**`RANGE` 30 已按本次要求更新**）……

经核对当前代码 `BigDogEntity.java`，四个常量**均未改动**：

- `RANGE = 20.0`（第 36 行），并非 30
- `CHARGING_TICKS = 60`、`FIRING_TICKS = 40`、`CHARGE_LOOP_INTERVAL = 20`（第 32-35 行）

计划中"（`RANGE` 30 已按本次要求更新）"这个括号注释与实际代码矛盾。实现者若据此认为 `RANGE` 已由 20 改到 30、无需再动，就会漏改，导致音波伤害射程与方块破坏距离仍为 20 而非 30。

建议：将第 140 行改为"固定 60t/40t/20t，`RANGE` 仍为 20.0（本次需一并提升至 30）"，并确认第 161 行"关键文件变更"中已包含 `RANGE` 的修改（当前第 161 行确实列了 `RANGE 30`，是正确目标，只是现状描述应纠正）。

---

## 其余核验（无问题）

- 第 72/154/161 行对单次播放、`CHARGE_LOOP_INTERVAL ≥122t`、`tryBreakBlock` `drop=false` 的表述一致。
- 第 155 行对 NBT 持久化范围与取消蓄力音频边界（方案 A）的描述准确，与 `cancelAttack()`/`world.playSound` 行为相符。
- 增量方案其余部分（转码规格、删除 MP3、122t/80t 对齐、30 段推进）与代码和需求一致。

## 结论

除上述 1 处"已勘察到现状"的 RANGE 描述偏差外，计划已完整、自洽、可进入实现。修正该括号注释即可闭环。
