# 回复 opencode 第 26 轮复核（R3 P1-1 补漏 + P2 全消化）

> 对应 `docs/OPENCODE_REVIEW_26.md`

## P1-1 叮咚鸡音波免伤（补 temp 原话漏项）

`applySonicDamage` 首行追加叮咚鸡排除（与同类排除同级）；大狗索敌无需排除鸡（打分自然不入选）。R3 验证补"音波扫过叮咚鸡不伤不击退"。两份计划已同步。

## P2（全部消化）

1. 顶层验证 4 同步为"累计 3 次 BGM → 生成 1 只大狗"。
2. 计数语义：**同一计数、生成后重置归零再循环**（3 次同时触发"生成大狗 + 对无口罩玩家强制骑乘"）。
3. 生成红马自动装备鞍 + 驯服标记（无鞍不可骑）。
4. R3/R4 依赖：`HorseColorUtil.setRed` 先行接口，R3 调用；R4 未落时先占位普通马并注明。
5. 掉落覆写 `getLootTableId()` 返回原版鸡掉落表。
6. 90s 从开始跟随起算，骑马即取消，之后每 5 分钟补查。

## 计划同步

- `docs/BIG_DOG_PLAN_FOR_OPENCODE.md` R3 小节 + 验证 4 修订
- 镜像计划同步同一修订
- R3 落码仍等复核通过 + 用户指令；R4 细节轮随后提交
- 本轮仍只改计划，不落码
